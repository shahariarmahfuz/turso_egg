services:
  - type: web
    name: egg-erp
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python pre_start.py && gunicorn wsgi:app"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.10
